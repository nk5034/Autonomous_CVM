apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cvm-catalyst-qa

resources:
  - namespace.yaml
  - ../../base

patchesStrategicMerge:
  - patch.yaml

images:
  - name: ghcr.io/REPLACE_ME/cvm-catalyst-backend
    newTag: qa-latest
  - name: ghcr.io/REPLACE_ME/cvm-catalyst-frontend
    newTag: qa-latest
